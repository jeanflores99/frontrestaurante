export interface EntityMesaDto {
    id: number,
    etiqueta: string,
    description?: string,
    free: boolean,
    total: number,
    piso: string,
    mozo: number
    location: string,
    active: boolean,
}